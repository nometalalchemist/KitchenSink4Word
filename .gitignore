# Environment / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Backups created by the server (slot folders since v1.6; the *.bak-* pattern
# stays for straggler files from the pre-v1.6 scheme)
.ks4w-backups/
*.bak-*.docx

# PRIVATE — never publish:
# The test corpus is real unpublished academic work (dissertation drafts,
# committee-revised manuscripts, reviewer comments). Tests skip cleanly when
# it is absent; drop your own .docx files here to run the full suite.
tests/corpus/*.docx

# Local build-planning notes (project-specific workflow details)
PLAN.md
V2_PLAN.md
dist/
build/
BUILD_LOG.md
V2_KICKOFF.md
V2_DESIGN.md
V1.5_KICKOFF.md
V1.6_KICKOFF.md
V1.6_SHIP_RUNBOOK.md
research/20260828_demand_mining.md
KitchenSink4Word-Community-Feedback.md
kitchensink4word-feedback.md
Developer Feedback/
integration/

# packed bundles ship as release assets, not commits
bundle/*.mcpb

# Private brand feedback (outside repo, defensive)
Brand Feedback/
V2.1_QUEUE.md
